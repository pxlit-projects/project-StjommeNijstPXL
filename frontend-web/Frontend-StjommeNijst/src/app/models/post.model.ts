export interface Post {
    id: number;  // ID of the post (matches the Long type in the backend)
    title: string;  // Title of the post
    content: string;  // Content of the post
    author: string;  // Author of the post
  }
  